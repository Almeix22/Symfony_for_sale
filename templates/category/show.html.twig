{% extends 'base.html.twig' %}

{% block title %}Hello AdvertisementController!{% endblock %}

{% block body %}
    <div class="container" style="background-color: white ; border-radius: 20px; padding: 20px">
    <p class="text-accent2">Annonce contenu dans la catégorie {{ categorie }} : </p>
    {% for advertisement in pagination.items %}
        <div style="display: flex; margin: 10px">
            <a class="btn btn-accent2" style="margin: 5px" href={{ path('app_advertisement_edit', {id : advertisement.id})}}>Edit </a>
            <a class="link-accent1" style="align-self: center" href={{ path('app_advertisement_show', {id : advertisement.id})}}> <div>{{ advertisement.title }}</div> </a>
        </div>
    {% endfor %}
    </div>
    {{ knp_pagination_render(pagination) }}
{% endblock %}
