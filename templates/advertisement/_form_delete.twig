<form action="{{ path('app_advertisement_delete', {'id': id}) }}" method="post">
    <input type="hidden" name="token" value="{{ csrf_token('delete-item') }}">
    <button type="submit" class="btn btn-accent2 btnDelete" onclick="return confirm('Êtes-vous sûr de vouloir supprimer cette annonce ?');">Delete</button>
</form>
