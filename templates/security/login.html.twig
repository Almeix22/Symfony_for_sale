{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}
<form method="post">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}
    <div class="container" style="background-color: white ; border-radius: 20px; padding: 20px; margin-top: 10px;">
        <h1 class="h3 mb-3 font-weight-normal">Connexion</h1>
        <label for="inputEmail">Email</label>
        <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>

        <label for="inputPassword" style="margin-top: 10px;">Mot de passe</label>
        <input type="password" value="" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>

        <input type="hidden" name="_csrf_token"
            value="{{ csrf_token('authenticate') }}"
        >

        <button class="btn btn-lg btn-primary connexion" style="margin-top: 20px;" type="submit">
            Connexion
        </button>
        <div style="margin-top: 10px">
            <p style="font-size: 16px; margin-bottom: 5px">Pas encore de compte ?</p>
            <a class="link-accent2" href="{{ path('app_register') }}">Créez votre compte ici.</a>
        </div>
    </div>
</form>
{% endblock %}
